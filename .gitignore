projects/
.env
.DS_Store
.claude/

# Locally-built rp-fuse binaries (built into the image via multi-stage Dockerfile,
# and cross-built for the host to power `ccr lint`).
rp-fuse/rp-fuse
rp-fuse/rp-fuse-darwin-arm64
